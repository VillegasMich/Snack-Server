export enum Rating {
  EXCELLENT,
  AVARAGE,
  BAD,
}

export const nameOptions = {
  maxLength: 20,
  minLength: 3,
};

export const descriptionOptions = {
  maxLength: 250,
};

export const instructionOptions = {
  maxLength: 500,
  minLength: 10,
};
